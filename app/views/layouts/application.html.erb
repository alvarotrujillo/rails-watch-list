<!DOCTYPE html>
<html>
  <head>
    <title>Film Watch Lists</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="wrapper">
      <div class="container">
        <div class="navbar-lewagon">
          <i class="fa-solid fa-film"></i> <%= link_to "Create Your Movie List", new_list_path %>
        </div>
      <%= yield %>
      </div>
    </div>
  </body>
</html>
